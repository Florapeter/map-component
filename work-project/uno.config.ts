import { defineConfig } from 'unocss'

export default defineConfig({
  // 自定义规则
  rules: [
    // 可以在这里添加自定义规则
  ],
  // 自定义快捷方式
  shortcuts: {
    // 可以在这里添加快捷方式
  },
  // 主题配置
  theme: {
    colors: {
      // 可以在这里添加自定义颜色
    }
  }
}) 